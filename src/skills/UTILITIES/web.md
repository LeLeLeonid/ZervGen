---
description: "Search the web and fetch URLs for real-time information"
tags: ["web", "search", "internet", "news", "url", "fetch", "browse", "google", "online"]
---
# WEB RESEARCH

Use `web_search(query)` to find information online via DuckDuckGo.
Use `fetch_url(url, parse_html=True)` to get and parse page content.

Always cite sources with URLs in your response.
For news, search with specific keywords and date references.
